# 异动捕手（stock_trace）优化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实施本计划。步骤用复选框（`- [ ]`）跟踪进度。

**Goal:** 按《异动捕手-优化点评估与设计方案.md》落地 8 个优化点中的 7 项可实施项（优化 7 轮询窗口已实现、无需改动），分三阶段交付：发布 gate + 校验纠错重试 + DLQ 管理（高）、并发检测 + 抓取重试（中）、增量采集 + 可观测性（按需）。

**Architecture:** 横跨两个仓库：aistock-app-api（Node，检测/事件/快照/入队）+ aistock-agent-py（Python，Stream 消费/LLM 归因/路由）。高优先级改动集中在「入队→发布→消费」链路的鲁棒性：Node 侧发布 gate 保证快照就绪才进 Stream，Python 侧为 `SNAPSHOT_NOT_READY` 加超时兜底、为校验失败加纠错重试、为 DLQ 加巡检/告警/重投。中优先级为性能与小改动。低优先级按需实施。

**Tech Stack:** TypeScript（Node, node:test + tsx）、Python（pytest, redis.asyncio, structlog, FastAPI）、PostgreSQL、Redis Stream。

---

## Global Constraints

（每任务的隐含要求，逐字来自设计方案/项目硬约束）

- 不推翻 2026-08-21「落定后归因一次」决策：检测/推送实时链路、三条落定路径（恢复窗口到期 / 反向落定 / 15:05 收盘兜底）保持不变。
- `publishPending` 的 Redis 发布失败必须保留 outbox `pending`，不得影响事件持久化与实时推送。
- 入队幂等由 `UNIQUE(event_id, trigger_revision, analysis_version, job_kind)` + `SELECT FOR UPDATE` 保证，任何改动不得破坏。
- Python Worker 只能按 `event_id`/`snapshot_id` 读 Node 冻结上下文，禁止直接抓 A 股数据源。
- 校验纠错**最多重试一次**（token 保护），不做无限重试；不改展示端 `getEffectiveArtifact` 回退逻辑。
- DLQ 重投必须带 error_code 白名单 + 单次批量上限，防止重投风暴。
- 异动写入并发必须复用现有事务 + UNIQUE 约束，禁止同股并发重复建事件。
- `cron.schedule()` 必须显式 `{ timezone: 'Asia/Shanghai' }`；本计划新增的定时巡检在 Python 侧用 asyncio 循环，不在 Node 加 cron。
- Node 代码禁止 `any`，用 `unknown`；沿用现有 `as`/`as unknown as` 风格。
- 所有结构化日志沿用 structlog 通道；指标统一进 `MetricsCollector`（observability/metrics.py），不另起炉灶。
- 每个任务结束时：`git commit`（若仓库干净则跳过）；任务末尾一次性追加对应仓库 `changelog-pending.md`。

---

## 阶段总览

| 阶段 | 内容 | 对应设计 |
|------|------|---------|
| 阶段一（高） | Task 1 发布 gate；Task 2 SNAPSHOT_NOT_READY 超时兜底；Task 3 校验纠错重试；Task 4 DLQ 巡检+告警+重投 | §3.1 / §3.3 / §3.2 |
| 阶段二（中） | Task 5 detect 并发分批；Task 6 事件抓取重试 | §4.1 / §4.3 |
| 阶段三（按需） | Task 7 revision 增量采集；Task 8 /metrics 指标与告警 | §4.2 / §5.2 |
| 收尾 | Task 9 文档与 changelog | §6 / 项目工作流 Phase 6 |

> 优化 7（轮询窗口）：已核实 `isWithinTradingWindows`（tradingTime.ts L41-47）排除午休与竞价间隙，**无改动**；自选股 > 数百只时把 `DETECTOR_INTERVAL_MS` 从 5000 调 10000（一行），先记入 backlog 不实施。

---

## File Structure

| 文件 | 责任 | 涉及任务 |
|------|------|---------|
| `aistock-app-api/src/modules/stock-trace/StockTraceJobService.ts` | 发布 gate：`held_until` 列 + `checkEnrichedSnapshotReady` | T1 |
| `aistock-app-api/src/modules/stock-trace/StockTraceService.ts` | `enqueueFinalAnalysis` 入队前确保 enriched 就绪；`triggerEventScrape` 重试；`captureSnapshots` 增量开关 | T1 / T6 / T7 |
| `aistock-app-api/src/modules/stock-trace/StockTraceSnapshotService.ts` | `scheduleEnriched` 完成冲刷 outbox；`captureEnriched/captureCorrected` 增量采集；`pickReusableSources` 纯函数 | T1 / T7 |
| `aistock-app-api/src/modules/stock-trace/PriceTriggerDetector.ts` | detect 并发分批 | T5 |
| `aistock-agent-py/src/aistock_agent/workers/stock_trace_consumer.py` | SNAPSHOT_NOT_READY 超时兜底；DLQ 巡检 + `replay_dlq` | T2 / T4 |
| `aistock-agent-py/src/aistock_agent/agents/workers/stock_trace.py` | LLM 校验纠错单次重试 | T3 |
| `aistock-agent-py/src/aistock_agent/observability/metrics.py` | stock_trace 业务计数器（一次性扩展） | T2（供 T3/T4/T8 消费） |
| `aistock-agent-py/src/aistock_agent/config.py` | 新增 stock_trace 超时/巡检配置项 | T2 / T4 |
| `aistock-agent-py/src/aistock_agent/api/routes.py` | `/admin/stock-trace/dlq/replay` + `/metrics` | T4 / T8 |
| 测试 | `final-attribution.spec.ts` / `eventScrapeTrigger.spec.ts` / 新增 `publish-gate.spec.ts`、`price-detector.spec.ts`、`snapshot-incremental.spec.ts`；Python `tests/unit/test_stock_trace.py` | 各任务 |

---

## 阶段一（高优先级）

### Task 1: 发布 gate —— 快照就绪才进 Stream

**Files:**
- Modify: `aistock-app-api/src/modules/stock-trace/StockTraceJobService.ts:29-58`（schema）、`:86-139`（publishPending）
- Modify: `aistock-app-api/src/modules/stock-trace/StockTraceSnapshotService.ts:343-348`（scheduleEnriched 冲刷 outbox）
- Modify: `aistock-app-api/src/modules/stock-trace/StockTraceService.ts:481-498`（enqueueFinalAnalysis 入队前确保就绪）
- Test: Create `aistock-app-api/src/modules/stock-trace/__tests__/publish-gate.spec.ts`

**Interfaces:**
- Consumes: `StockTraceSnapshotService.getAnalysisContext(eventId, revision)`（已存在）、`StockTraceSnapshotService.captureEnriched(event)`（已存在）、`StockTraceService.getTriggerEvent(eventId, revision)`（已存在）、`redis.xadd`（core/redis）
- Produces: `StockTraceJobService.publishPending()` 增加 gate 语义；outbox 新增 `held_until` 列；`scheduleEnriched(event, incremental=false)` 完成后自动冲刷 outbox

- [ ] **Step 1: 写失败测试**

新建 `publish-gate.spec.ts`，用 node:test mock 数据库与 Redis。gate 未就绪 → 不调 `xadd`、outbox 被 `UPDATE ... held_until`；gate 就绪 → 调 `xadd`。

```typescript
// src/modules/stock-trace/__tests__/publish-gate.spec.ts
// 运行：node --import tsx --test src/modules/stock-trace/__tests__/publish-gate.spec.ts
import assert from 'node:assert/strict';
import { afterEach, describe, it, mock } from 'node:test';
import pool from '../../../core/db';
import redis from '../../../core/redis';
import { StockTraceJobService } from '../StockTraceJobService';

function pendingRow() {
    return {
        outbox_id: 'outbox-1',
        job_id: 'job-1',
        payload: { eventId: 'mv:600000:2026-08-21:1:up', triggerRevision: 1, analysisVersion: 'llm-stock-trace-v1' },
    };
}

afterEach(() => { mock.restoreAll(); });

describe('publishPending 快照就绪 gate', () => {
    it('enriched 快照未就绪 → 不发布、outbox 置 held_until 留待下轮', async () => {
        mock.method(pool, 'query', async (sql: string | { text: string }) => {
            const text = typeof sql === 'string' ? sql : sql.text;
            // 待发布列表
            if (text.includes('FROM stock_trace_outbox') && text.includes("status = 'pending'")) {
                return { rows: [pendingRow()] };
            }
            // enriched 快照检查：无行 → 未就绪
            if (text.includes("snapshot_stage = 'enriched'")) {
                return { rows: [] };
            }
            // held_until 更新 / ensureSchema DDL
            return { rows: [] };
        });
        const xadd = mock.method(redis, 'xadd', async () => '0-0');

        const result = await StockTraceJobService.publishPending();

        assert.deepEqual(result, { published: 0, failed: 0 });
        assert.equal(xadd.mock.callCount(), 0, '未就绪不应发布到 Stream');
    });

    it('enriched 快照就绪 → 发布到 Stream 并更新 job 状态', async () => {
        const updates: Array<{ text: string; params: unknown[] }> = [];
        mock.method(pool, 'query', async (sql: string | { text: string }, ...params: unknown[]) => {
            const text = typeof sql === 'string' ? sql : sql.text;
            if (text.includes('FROM stock_trace_outbox') && text.includes("status = 'pending'")) {
                return { rows: [pendingRow()] };
            }
            if (text.includes("snapshot_stage = 'enriched'")) {
                return { rows: [{ snapshot_id: 'snap-1' }] }; // 就绪
            }
            if (text.includes('UPDATE stock_trace_outbox')) {
                updates.push({ text, params });
                return { rowCount: 1 };
            }
            if (text.includes('UPDATE stock_trace_jobs')) {
                return { rowCount: 1 };
            }
            return { rows: [] };
        });
        mock.method(redis, 'xadd', async () => '1680000000000-0');

        const result = await StockTraceJobService.publishPending();

        assert.deepEqual(result, { published: 1, failed: 0 });
        assert.ok(updates.some(u => u.text.includes('status = \'published\'')), '就绪时 outbox 应置 published');
    });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `node --import tsx --test src/modules/stock-trace/__tests__/publish-gate.spec.ts`
Expected: FAIL（当前 `publishPending` 无 gate，未就绪也直接 `xadd`，第一个用例的 `xadd` 计数断言失败）。

- [ ] **Step 3: 实现 gate**

`StockTraceJobService.ts` 顶部加常量：

```typescript
const HELD_RECHECK_INTERVAL_MS = 5_000; // 未就绪重查间隔（5s）
const MAX_HOLD_SECONDS = 60;             // 硬超时：超过后不再 hold，直接发布交由 consumer 兜底
```

`ensureSchema()` 内（`stock_trace_outbox` 建表后）追加幂等列：

```typescript
await pool.query('ALTER TABLE stock_trace_outbox ADD COLUMN IF NOT EXISTS held_until TIMESTAMPTZ');
```

`publishPending()` 的待发布查询加 gate 过滤（超时后不再 hold）：

```typescript
const rows = await pool.query<PendingOutboxRow>(`
    SELECT outbox_id, job_id, payload FROM stock_trace_outbox
    WHERE status = 'pending'
      AND (held_until IS NULL OR held_until <= CURRENT_TIMESTAMP
           OR created_at <= CURRENT_TIMESTAMP - interval '60 seconds')
    ORDER BY created_at
    LIMIT $1
`, [Math.min(Math.max(limit, 1), 200)]);
```

循环内、`xadd` 之前插入 gate 检查：

```typescript
for (const row of rows.rows) {
    try {
        // 快照 gate：enriched 未就绪则不发布，置 held_until 5s 后重查；
        // 超过 MAX_HOLD_SECONDS 的 job 不再 hold，直接发布（由 consumer SNAPSHOT_TIMEOUT 兜底）。
        const ready = await this.checkEnrichedSnapshotReady(row.payload.eventId, row.payload.triggerRevision);
        if (!ready) {
            await pool.query(`
                UPDATE stock_trace_outbox
                SET held_until = CASE WHEN created_at > CURRENT_TIMESTAMP - interval '60 seconds'
                                      THEN CURRENT_TIMESTAMP + interval '5 seconds'
                                      ELSE NULL END,
                    updated_at = CURRENT_TIMESTAMP
                WHERE outbox_id = $1 AND status = 'pending'
            `, [row.outbox_id]);
            continue;
        }
        const streamMessageId = await redis.xadd(
            /* 现有 xadd 参数不变 */
            ...
        );
        // 其余逻辑不变
    }
}
```

新增私有静态方法（放 `publishPending` 之后）：

```typescript
private static async checkEnrichedSnapshotReady(eventId: string, triggerRevision: number): Promise<boolean> {
    const result = await pool.query<{ snapshot_id: string }>(`
        SELECT snapshot_id FROM stock_trace_snapshots
        WHERE event_id = $1 AND trigger_revision = $2 AND snapshot_stage = 'enriched'
        ORDER BY captured_at DESC LIMIT 1
    `, [eventId, triggerRevision]);
    return result.rows.length > 0;
}
```

`StockTraceSnapshotService.ts` 顶部加导入（无循环依赖：JobService 只依赖 core/db + core/redis）：

```typescript
import { StockTraceJobService } from './StockTraceJobService';
```

`scheduleEnriched` 改为：captureEnriched 完成后冲刷一次 outbox（让被 gate 延后的 job 及时发布）：

```typescript
static scheduleEnriched(event: TriggerEvent, incremental = false): void {
    const timer = setTimeout(() => {
        void this.captureEnriched(event, incremental).then(() =>
            StockTraceJobService.publishPending(),
        ).catch((error: unknown) => {
            console.error('[StockTraceSnapshot] enriched capture failed:', error instanceof Error ? error.message : error);
        });
    }, 1_000);
    timer.unref();
}
```

`StockTraceService.enqueueFinalAnalysis` 在**无 client**（恢复窗口到期 / 收盘兜底）路径入队前确保 enriched 就绪（缺失则同步采集兜底；事务外执行不阻塞短事务）：

```typescript
private static async enqueueFinalAnalysis(eventId: string, triggerRevision: number, client?: PoolClient): Promise<void> {
    await this.ensureSchema();
    if (client) {
        await StockTraceJobService.enqueue(client, { eventId, triggerRevision });
        return;
    }
    // 无 client 路径：入队前确保 enriched 快照就绪（缺失则同步采集一次兜底），
    // 避免 job 发布后 consumer 反复拿 SNAPSHOT_NOT_READY 空转。事务外执行。
    await this.ensureEnrichedReady(eventId, triggerRevision);
    const conn = await pool.connect();
    try {
        await conn.query('BEGIN');
        await StockTraceJobService.enqueue(conn, { eventId, triggerRevision });
        await conn.query('COMMIT');
    } catch (error) {
        await conn.query('ROLLBACK');
        throw error;
    } finally {
        conn.release();
    }
}

private static async ensureEnrichedReady(eventId: string, triggerRevision: number): Promise<void> {
    const context = await StockTraceSnapshotService.getAnalysisContext(eventId, triggerRevision);
    if (context) return;
    const event = await this.getTriggerEvent(eventId, triggerRevision);
    if (!event) return; // 事件不存在：交由 consumer 超时兜底
    await StockTraceSnapshotService.captureEnriched(event);
}
```

> 说明：反向落定路径（`client` 非空，位于 `processPriceFact` 事务内）不调用 `ensureEnrichedReady`，避免在事务内阻塞等待外部采集（最坏 25s）；该路径依赖 Task 2 的 consumer 超时兜底。正常恢复窗口（5 分钟）内 enriched 已由 `scheduleEnriched` 完成，此函数为快速 no-op。

- [ ] **Step 4: 运行测试验证通过**

Run: `node --import tsx --test src/modules/stock-trace/__tests__/publish-gate.spec.ts`
Expected: PASS（2 个用例）。

Run 全量回归 + 类型检查：

```bash
node --import tsx --test src/modules/stock-trace/__tests__/*.spec.ts
npx tsc --noEmit
```

Expected: 全部 PASS；tsc 无错误。

- [ ] **Step 5: Commit**

```bash
git add src/modules/stock-trace/StockTraceJobService.ts src/modules/stock-trace/StockTraceSnapshotService.ts src/modules/stock-trace/StockTraceService.ts src/modules/stock-trace/__tests__/publish-gate.spec.ts
git commit -m "feat(stock-trace): publish gate - only publish job after enriched snapshot ready"
```

---

### Task 2: Consumer `SNAPSHOT_NOT_READY` 超时兜底 + stock_trace 指标

**Files:**
- Modify: `aistock-agent-py/src/aistock_agent/workers/stock_trace_consumer.py:91-125`（_consume_message）、`:154-165`（run_forever 清理）
- Modify: `aistock-agent-py/src/aistock_agent/config.py`（新增超时配置）
- Modify: `aistock-agent-py/src/aistock_agent/observability/metrics.py`（一次性新增 stock_trace 计数器，供 T3/T4/T8 消费）
- Test: `aistock-agent-py/tests/unit/test_stock_trace.py`

**Interfaces:**
- Consumes: `settings.stock_trace_snapshot_not_ready_timeout_seconds`（本任务新增）、`get_metrics_collector()`（已存在）
- Produces: `MetricsCollector.record_stock_trace_snapshot_not_ready()` / `record_stock_trace_validation_failed(error_type)` / `record_stock_trace_validation_retry_success()` / `record_stock_trace_dlq_total(error_code)`（后三者供 T3/T4 使用）；Redis key `stock_trace:snapshot_pending:{job_id}`（TTL 3600）

- [ ] **Step 1: 写失败测试**

在 `tests/unit/test_stock_trace.py` 追加。先扩展 FakeRedis 支持 `get/setex/delete/xadd/xrange/xdel`（供本任务与 Task 4 复用）：

```python
class FakeRedis:
    def __init__(self) -> None:
        self.acked: list[tuple[str, str, str]] = []
        self.store: dict[str, str] = {}
        self.added: list[tuple[str, dict[bytes, bytes]]] = []

    async def xack(self, stream: str, group: str, message_id: str) -> int:
        self.acked.append((stream, group, message_id))
        return 1

    async def get(self, key: str) -> str | None:
        return self.store.get(key)

    async def setex(self, key: str, ttl: int, value: str) -> None:
        del ttl
        self.store[key] = value

    async def delete(self, *keys: str) -> int:
        removed = sum(1 for k in keys if k in self.store)
        for k in keys:
            self.store.pop(k, None)
        return removed

    async def xadd(self, stream: str, payload: dict[bytes, bytes]) -> str:
        self.added.append((stream, payload))
        return "dlq-0"
```

新增用例（放在现有 `test_consumer_*` 之后）：

```python
class SnapshotNotReadyWorker:
    async def analyze(self, _e: str, _r: int, _v: str) -> StockTraceWorkerOutcome:
        return StockTraceWorkerOutcome(status="failed", error_code="SNAPSHOT_NOT_READY")


@pytest.mark.asyncio
async def test_consumer_snapshot_not_ready_first_seen_does_not_dead_letter() -> None:
    redis_client = FakeRedis()
    consumer = StockTraceConsumer(
        redis_client,  # type: ignore[arg-type]
        StockTraceNodeClient(FakeNodeClient()),
        SnapshotNotReadyWorker(),  # type: ignore[arg-type]
    )
    # 首次命中：标记 first_seen，不 dead-letter，不 ack
    await consumer._consume_message("m-1", {
        "job_id": "job-1",
        "event_id": "mv:000004:2026-07-30:1:up",
        "trigger_revision": "1",
        "analysis_version": "llm-stock-trace-v1",
    })
    assert redis_client.acked == []
    assert redis_client.store["stock_trace:snapshot_pending:job-1"]


@pytest.mark.asyncio
async def test_consumer_snapshot_not_ready_timeout_dead_letters() -> None:
    redis_client = FakeRedis()
    # 模拟 first_seen 已超过阈值（10 分钟前）
    redis_client.store["stock_trace:snapshot_pending:job-1"] = str(int(time.time()) - 601)
    consumer = StockTraceConsumer(
        redis_client,  # type: ignore[arg-type]
        StockTraceNodeClient(FakeNodeClient()),
        SnapshotNotReadyWorker(),  # type: ignore[arg-type]
    )
    await consumer._consume_message("m-1", {
        "job_id": "job-1",
        "event_id": "mv:000004:2026-07-30:1:up",
        "trigger_revision": "1",
        "analysis_version": "llm-stock-trace-v1",
    })
    assert redis_client.added, "超时应进 DLQ"
    assert redis_client.added[0][0] == "stock-trace.jobs.dlq"
    assert redis_client.added[0][1][b"error_code"] == b"SNAPSHOT_TIMEOUT"
```

> 注：需要在文件顶部补 `import time`（当前已 import）。

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/unit/test_stock_trace.py -v`
Expected: 新增 2 用例 FAIL（当前 `_consume_message` 对 SNAPSHOT_NOT_READY 直接 return，无标记/无超时判定）。

- [ ] **Step 3: 实现**

`config.py` 的 stock_trace 配置段新增：

```python
# SNAPSHOT_NOT_READY 空转兜底：首次命中起超过该秒数仍未就绪 → SNAPSHOT_TIMEOUT 死信
stock_trace_snapshot_not_ready_timeout_seconds: int = 600
```

`observability/metrics.py`：`__init__` 增加计数器字段（一次加齐，供 T3/T4/T8 消费）：

```python
# Stock Trace 链路指标
self._stock_trace_snapshot_not_ready = 0
self._stock_trace_validation_failed: dict[str, int] = {}
self._stock_trace_validation_retry_success = 0
self._stock_trace_dlq_total: dict[str, int] = {}
```

新增方法（放在 `record_chat_qa_escalation` 之后）：

```python
def record_stock_trace_snapshot_not_ready(self) -> None:
    with self._lock:
        self._stock_trace_snapshot_not_ready += 1

def record_stock_trace_validation_failed(self, error_type: str = "") -> None:
    with self._lock:
        self._stock_trace_validation_failed[error_type] = (
            self._stock_trace_validation_failed.get(error_type, 0) + 1
        )

def record_stock_trace_validation_retry_success(self) -> None:
    with self._lock:
        self._stock_trace_validation_retry_success += 1

def record_stock_trace_dlq_total(self, error_code: str) -> None:
    with self._lock:
        self._stock_trace_dlq_total[error_code] = (
            self._stock_trace_dlq_total.get(error_code, 0) + 1
        )
```

`get_metrics()` 的 snapshot 返回值加入：

```python
stock_trace_snapshot_not_ready = self._stock_trace_snapshot_not_ready
stock_trace_validation_failed = dict(self._stock_trace_validation_failed)
stock_trace_validation_retry_success = self._stock_trace_validation_retry_success
stock_trace_dlq_total = dict(self._stock_trace_dlq_total)
```

并在返回 dict 中加入 `"stock_trace": {...}` 嵌套段；`reset()` 同步清零。

`stock_trace_consumer.py`：顶部导入 metrics：

```python
from aistock_agent.observability.metrics import get_metrics_collector

_metrics = get_metrics_collector()
```

`_consume_message` 的 SNAPSHOT_NOT_READY 分支改为带超时判定：

```python
if outcome.error_code == "SNAPSHOT_NOT_READY":
    await self._node_client.report_job(job_id, "queued", error_code=outcome.error_code)
    _metrics.record_stock_trace_snapshot_not_ready()
    # 超时兜底：首次命中起超过阈值仍未就绪 → SNAPSHOT_TIMEOUT 死信，杜绝无限空转
    if await self._snapshot_pending_expired(job_id):
        await self._dead_letter(message_id, fields, "SNAPSHOT_TIMEOUT")
        return
    # 不确认消息：快照就绪后由 pending reclaim 重新执行。
    return
```

completed 分支（`_consume_message` 内 `writeback` 成功处）清理 pending 标记：

```python
if writeback is not None:
    await self._node_client.report_job(job_id, "completed")
    await self._redis.xack(STREAM, self._group, message_id)
    await self._redis.delete(f"stock_trace:snapshot_pending:{job_id}")
    logger.info("stock_trace_job_completed", job_id=job_id, event_id=event_id)
    return
```

新增私有方法（放在 `_handle_failure` 前）：

```python
async def _snapshot_pending_expired(self, job_id: str) -> bool:
    """记录首次 SNAPSHOT_NOT_READY 时间；超过阈值返回 True（由调用方死信）。"""
    key = f"stock_trace:snapshot_pending:{job_id}"
    raw = await self._redis.get(key)
    now = int(time.time())
    if raw is None:
        await self._redis.setex(key, 3600, str(now))
        return False
    elapsed = now - int(raw)
    if elapsed > settings.stock_trace_snapshot_not_ready_timeout_seconds:
        logger.warning("stock_trace_snapshot_timeout", job_id=job_id, elapsed_seconds=elapsed)
        return True
    return False
```

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/unit/test_stock_trace.py -v`
Expected: 全部 PASS（含新增 2 用例与既有用例）。

- [ ] **Step 5: Commit**

```bash
git add src/aistock_agent/workers/stock_trace_consumer.py src/aistock_agent/config.py src/aistock_agent/observability/metrics.py tests/unit/test_stock_trace.py
git commit -m "feat(stock-trace): SNAPSHOT_NOT_READY timeout dead-letter + stock_trace metrics"
```

---

### Task 3: LLM 校验失败纠错单次重试

**Files:**
- Modify: `aistock-agent-py/src/aistock_agent/agents/workers/stock_trace.py:166-249`（analyze 重试）
- Test: `aistock-agent-py/tests/unit/test_stock_trace.py`

**Interfaces:**
- Consumes: `validate_stock_trace_result` / `StockTraceValidationError`（已存在）、`_metrics.record_stock_trace_validation_failed(error_type)` / `record_stock_trace_validation_retry_success()`（Task 2 提供）
- Produces: `StockTraceWorker.analyze` 在 `ValueError`/`StockTraceValidationError` 时最多重试一次（带纠错提示），仍失败返回 `VALIDATION_REJECTED`；非 `ValueError` 异常仍立即返回 `LLM_OR_DEPENDENCY_UNAVAILABLE`

- [ ] **Step 1: 写失败测试**

在 `tests/unit/test_stock_trace.py` 追加三类 FakeLlm 与用例：

```python
class FakeLlmFailsFirst:
    calls = 0
    structured_schema: type[object] | None = None

    def with_structured_output(self, schema: type[object], *, method: str, include_raw: bool = False) -> "FakeLlmFailsFirst":
        self.__class__.structured_schema = schema
        return self

    async def ainvoke(self, _messages: list[object]) -> StockTraceResultPayload:
        self.__class__.calls += 1
        if self.__class__.calls == 1:
            raise StockTraceValidationError("fact node requires evidence")
        return StockTraceResultPayload.model_validate(
            valid_result().model_dump(exclude={"schema_version", "event_id", "snapshot_id", "analysis_version"})
        )


class FakeLlmAlwaysFails:
    calls = 0

    def with_structured_output(self, schema: type[object], *, method: str, include_raw: bool = False) -> "FakeLlmAlwaysFails":
        return self

    async def ainvoke(self, _messages: list[object]) -> StockTraceResultPayload:
        self.__class__.calls += 1
        raise StockTraceValidationError("confirmed requires high confidence")


class FakeLlmGenericError:
    calls = 0

    def with_structured_output(self, schema: type[object], *, method: str, include_raw: bool = False) -> "FakeLlmGenericError":
        return self

    async def ainvoke(self, _messages: list[object]) -> StockTraceResultPayload:
        self.__class__.calls += 1
        raise RuntimeError("provider down")


@pytest.mark.asyncio
async def test_worker_retries_once_on_validation_error_then_succeeds() -> None:
    FakeLlmFailsFirst.calls = 0
    worker = StockTraceWorker(StockTraceNodeClient(FakeNodeClient()), llm_factory=FakeLlmFailsFirst)
    outcome = await worker.analyze("mv:000004:2026-07-30:1:up", 1, "llm-stock-trace-v1")
    assert outcome.status == "completed"
    assert outcome.result is not None
    assert FakeLlmFailsFirst.calls == 2, "校验失败应带纠错提示重试一次"


@pytest.mark.asyncio
async def test_worker_retries_once_then_fails_with_validation_rejected() -> None:
    FakeLlmAlwaysFails.calls = 0
    worker = StockTraceWorker(StockTraceNodeClient(FakeNodeClient()), llm_factory=FakeLlmAlwaysFails)
    outcome = await worker.analyze("mv:000004:2026-07-30:1:up", 1, "llm-stock-trace-v1")
    assert outcome.status == "failed"
    assert outcome.error_code == "VALIDATION_REJECTED"
    assert FakeLlmAlwaysFails.calls == 2, "最多重试一次"


@pytest.mark.asyncio
async def test_worker_does_not_retry_generic_llm_error() -> None:
    FakeLlmGenericError.calls = 0
    worker = StockTraceWorker(StockTraceNodeClient(FakeNodeClient()), llm_factory=FakeLlmGenericError)
    outcome = await worker.analyze("mv:000004:2026-07-30:1:up", 1, "llm-stock-trace-v1")
    assert outcome.status == "failed"
    assert outcome.error_code == "LLM_OR_DEPENDENCY_UNAVAILABLE"
    assert FakeLlmGenericError.calls == 1, "非校验异常不重试"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/unit/test_stock_trace.py -v`
Expected: 新增 3 用例 FAIL（当前单次调用，calls==1）。

- [ ] **Step 3: 实现**

`workers/stock_trace.py` 顶部导入 metrics：

```python
from aistock_agent.observability.metrics import get_metrics_collector

_metrics = get_metrics_collector()
```

将 `analyze` 的 LLM 调用段重构为「最多 2 次尝试」（首次 + 纠错一次）。替换 `analyze` 从 `llm = self._llm_factory()` 到 `validate_stock_trace_result(...)` 之间的逻辑：

```python
llm = self._llm_factory()
structured_llm = llm.with_structured_output(
    StockTraceResultPayload,
    method=settings.stock_trace_structured_output_method,
    include_raw=True,
)
last_error: str | None = None
for attempt in range(2):  # 首次 + 校验失败纠错一次（token 保护，不无限重试）
    try:
        messages: list[object] = [
            SystemMessage(content=STOCK_TRACE_PROMPT),
            HumanMessage(content=snapshot.model_dump_json()),
        ]
        if last_error is not None:
            messages.append(HumanMessage(
                content=(
                    "你上一轮输出未通过确定性校验，请修正后重试。"
                    f"校验错误详情：{last_error}\n"
                    "请对照上面错误逐项修正（缺失字段补齐、未引用的 source_id 删除、"
                    "选中链补全六阶段、confirmed 条件不满足时降级为 probable/insufficient）。"
                )
            ))
        response = await structured_llm.ainvoke(messages)
        # ... 以下解析逻辑与现有完全一致（response 分支 / _recover_tool_payload /
        #     _strip_post_window_market_references / primary_chain_id 派生 / role 派生）...
        result = StockTraceResult.model_validate({
            "schema_version": "stock-trace-result-v1",
            "event_id": event_id,
            "snapshot_id": snapshot.snapshot_id,
            "analysis_version": analysis_version,
            **payload_data,
        })
        validate_stock_trace_result(result, snapshot)
        if attempt == 1:
            _metrics.record_stock_trace_validation_retry_success()
        return StockTraceWorkerOutcome(status="completed", result=result)
    except (ValueError, StockTraceValidationError) as exc:
        # 解析/校验类失败：记录 + 纠错重试一次（第二次循环直接落到返回值）
        _metrics.record_stock_trace_validation_failed(type(exc).__name__)
        logger.warning(
            "stock_trace_validation_failed",
            event_id=event_id, attempt=attempt + 1, error=str(exc),
        )
        last_error = str(exc)
        continue
return StockTraceWorkerOutcome(status="failed", error_code="VALIDATION_REJECTED")
```

> 原 `except Exception` 分支（非 ValueError，返回 `LLM_OR_DEPENDENCY_UNAVAILABLE`）保留在循环外层：由于 `ValueError`/`StockTraceValidationError` 已在循环内捕获，外层 `except Exception` 只接其余异常并直接 return，不重试。

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/unit/test_stock_trace.py -v`
Expected: 全部 PASS（新增 3 用例 + 既有 `test_worker_returns_validated_structured_result` 首轮即通过、calls==1，不受影响）。

- [ ] **Step 5: Commit**

```bash
git add src/aistock_agent/agents/workers/stock_trace.py tests/unit/test_stock_trace.py
git commit -m "feat(stock-trace): single corrective retry on validation rejection + metrics"
```

---

### Task 4: DLQ 巡检 + 告警 + 手动重投接口

**Files:**
- Modify: `aistock-agent-py/src/aistock_agent/workers/stock_trace_consumer.py`（`_dead_letter` 记指标、新增 `replay_dlq`、run_forever 巡检）
- Modify: `aistock-agent-py/src/aistock_agent/config.py`（巡检间隔/告警阈值）
- Modify: `aistock-agent-py/src/aistock_agent/api/routes.py`（`/admin/stock-trace/dlq/replay`）
- Test: `aistock-agent-py/tests/unit/test_stock_trace.py`

**Interfaces:**
- Consumes: `_metrics.record_stock_trace_dlq_total(error_code)`（Task 2 提供）、`settings.stock_trace_dlq_inspect_interval_seconds` / `stock_trace_dlq_alert_after_seconds`（本任务新增）
- Produces: 模块级 `replay_dlq(redis_client, filter_criteria=None, limit=50) -> int`（路由与测试均可调用）；`REPLAYABLE_ERROR_CODES` 白名单；`POST /admin/stock-trace/dlq/replay`

- [ ] **Step 1: 写失败测试**

在 `tests/unit/test_stock_trace.py` 追加（FakeRedis 已支持 `xrange`/`xdel` 吗？——本任务需扩展 FakeRedis 增加 `xrange`/`xdel`）：

```python
# FakeRedis 追加：
async def xrange(self, stream: str, min: str = "-", max: str = "+", count: int | None = None):
    del min, max
    return [
        (msg_id, {k.encode(): v.encode() for k, v in payload.items()})
        for msg_id, payload in self.dlq  # 见下方 dlq 列表
    ]

async def xdel(self, stream: str, *message_ids: str) -> int:
    before = len(self.dlq)
    self.dlq = [(m, p) for m, p in self.dlq if m not in message_ids]
    return before - len(self.dlq)
```

并在 `FakeRedis.__init__` 增加 `self.dlq: list[tuple[str, dict[str, str]]] = []`，`xadd` 改为向 `dlq` 写（或保留原 `added` 同时写 `dlq`）。为保证既有用例不破坏，建议新增独立 `FakeRedisForDlq`：

```python
class FakeRedisForDlq:
    def __init__(self) -> None:
        self.dlq: list[tuple[str, dict[str, str]]] = []
        self.main: list[tuple[str, dict[str, str]]] = []

    async def xrange(self, stream: str, min: str = "-", max: str = "+", count: int | None = None):
        del stream, min, max
        return [(m, {k.encode(): v.encode() for k, v in p.items()}) for m, p in self.dlq[:count or len(self.dlq)]]

    async def xadd(self, stream: str, payload: dict[bytes, bytes]) -> str:
        self.main.append((stream, {k.decode(): v.decode() for k, v in payload.items()}))
        return "new-0"

    async def xdel(self, stream: str, *message_ids: str) -> int:
        before = len(self.dlq)
        self.dlq = [(m, p) for m, p in self.dlq if m not in message_ids]
        return before - len(self.dlq)


@pytest.mark.asyncio
async def test_replay_dlq_moves_messages_back_to_main_stream() -> None:
    from aistock_agent.workers.stock_trace_consumer import replay_dlq
    redis_client = FakeRedisForDlq()
    redis_client.dlq = [
        ("dlq-1", {"job_id": "job-1", "event_id": "e1", "error_code": "SNAPSHOT_TIMEOUT"}),
        ("dlq-2", {"job_id": "job-2", "event_id": "e2", "error_code": "INVALID_JOB_MESSAGE"}),
    ]
    replayed = await replay_dlq(redis_client, {"error_code": "SNAPSHOT_TIMEOUT"}, limit=10)
    assert replayed == 1
    assert redis_client.main[0][1]["job_id"] == "job-1"
    assert len(redis_client.dlq) == 1


@pytest.mark.asyncio
async def test_replay_dlq_rejects_non_replayable_error_code() -> None:
    from aistock_agent.workers.stock_trace_consumer import replay_dlq
    redis_client = FakeRedisForDlq()
    redis_client.dlq = [("dlq-1", {"job_id": "job-1", "error_code": "INVALID_JOB_MESSAGE"})]
    replayed = await replay_dlq(redis_client, None, limit=10)
    assert replayed == 0, "结构性错误不允许重投"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/unit/test_stock_trace.py -v`
Expected: 新增 2 用例 FAIL（`replay_dlq` 未定义）。

- [ ] **Step 3: 实现**

`config.py` 新增：

```python
# DLQ 巡检：每隔 N 秒检查一次；DLQ 长度>0 持续超过该秒数触发告警
stock_trace_dlq_inspect_interval_seconds: int = 60
stock_trace_dlq_alert_after_seconds: int = 900
```

`stock_trace_consumer.py`：定义白名单与模块级重投函数（放在 `_fields` 之后、类之前）：

```python
# 可重投错误码白名单：结构性错误（INVALID_JOB_MESSAGE 等）不允许重投
REPLAYABLE_ERROR_CODES = {
    "SNAPSHOT_TIMEOUT",
    "NODE_WRITEBACK_FAILED",
    "WORKER_FAILED",
    "LLM_OR_DEPENDENCY_UNAVAILABLE",
}


async def replay_dlq(
    redis_client: aioredis.Redis,
    filter_criteria: dict[str, str] | None = None,
    limit: int = 50,
) -> int:
    """把 DLQ 中可重投消息 re-xadd 回主流并删除。结构性错误码直接跳过。"""
    filter_criteria = filter_criteria or {}
    messages = await redis_client.xrange(DLQ_STREAM, min="-", max="+", count=max(1, limit))
    replayed = 0
    for message_id, raw_fields in messages:
        fields = _fields(raw_fields)
        error_code = fields.get("error_code", "")
        if error_code not in REPLAYABLE_ERROR_CODES:
            continue
        if filter_criteria.get("error_code") and error_code != filter_criteria["error_code"]:
            continue
        if filter_criteria.get("job_id") and fields.get("job_id") != filter_criteria["job_id"]:
            continue
        payload: dict[EncodableT, EncodableT] = {}
        for key, value in fields.items():
            if key == "error_code":
                continue  # 重投时剔除死信元信息
            payload[key] = value
        await redis_client.xadd(STREAM, payload)
        await redis_client.xdel(DLQ_STREAM, message_id)
        replayed += 1
        if replayed >= limit:
            break
    if replayed:
        logger.info("stock_trace_dlq_replayed", count=replayed)
    return replayed
```

`_dead_letter` 末尾追加指标：

```python
await self._redis.xack(STREAM, self._group, message_id)
_metrics.record_stock_trace_dlq_total(error_code)
logger.warning("stock_trace_job_dead_letter", job_id=job_id, error_code=error_code)
```

`run_forever` 加巡检（DLQ 长度 > 0 且距上次告警超阈值 → 结构化告警）：

```python
async def run_forever(self) -> None:
    global _stock_trace_consumer_last_heartbeat
    _stock_trace_consumer_last_heartbeat = time.time()
    last_inspect = 0.0
    last_alert = 0.0
    while True:
        _stock_trace_consumer_last_heartbeat = time.time()
        try:
            now = time.time()
            if now - last_inspect >= settings.stock_trace_dlq_inspect_interval_seconds:
                last_inspect = now
                length = await self._redis.xlen(DLQ_STREAM)
                if length > 0 and now - last_alert >= settings.stock_trace_dlq_alert_after_seconds:
                    last_alert = now
                    logger.warning(
                        "stock_trace_dlq_alert",
                        dlq_length=length,
                        dlq_persist_seconds=int(now - last_alert),
                        hint="run POST /admin/stock-trace/dlq/replay to requeue",
                    )
            await self.consume_once()
        except asyncio.CancelledError:
            raise
        except Exception:
            logger.exception("stock_trace_consumer_iteration_failed")
            await asyncio.sleep(1)
```

`api/routes.py`：新增管理端点（放在 `/admin/trigger/review_quick` 附近，沿用 `verify_internal_token` 风格）：

```python
@router.post("/admin/stock-trace/dlq/replay")
async def stock_trace_dlq_replay(
    body: dict[str, object] | None = None,
    _: None = Depends(verify_internal_token),
) -> dict[str, object]:
    """手动把 stock-trace DLQ 可重投消息放回主流（结构性错误码拒绝）。
    支持过滤 error_code / job_id，单次上限 50 防重投风暴。
    """
    from aistock_agent.workers.stock_trace_consumer import replay_dlq
    from aistock_agent.workers.stock_trace_consumer import REPLAYABLE_ERROR_CODES

    body = body or {}
    filter_criteria: dict[str, str] = {}
    raw_filter = body.get("filter")
    if isinstance(raw_filter, dict):
        filter_criteria = {str(k): str(v) for k, v in raw_filter.items()}
    error_code = filter_criteria.get("error_code")
    if error_code and error_code not in REPLAYABLE_ERROR_CODES:
        raise HTTPException(
            status_code=400,
            detail=f"error_code {error_code} is not replayable; allowed: {sorted(REPLAYABLE_ERROR_CODES)}",
        )
    limit = min(int(body.get("limit", 50)), 50)
    redis_client = aioredis.from_url(settings.stock_trace_redis_url)
    try:
        replayed = await replay_dlq(redis_client, filter_criteria, limit)
    finally:
        await redis_client.aclose()
    logger = structlog.get_logger()
    logger.info("stock_trace_dlq_replay_manual", replayed=replayed, filter=filter_criteria)
    return {"replayed": replayed, "filter": filter_criteria}
```

> 需确认 `routes.py` 已导入 `aioredis`；未导入时在函数内 `import redis.asyncio as aioredis`。

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/unit/test_stock_trace.py -v`
Expected: 全部 PASS（含新增 2 用例）。

- [ ] **Step 5: Commit**

```bash
git add src/aistock_agent/workers/stock_trace_consumer.py src/aistock_agent/config.py src/aistock_agent/api/routes.py tests/unit/test_stock_trace.py
git commit -m "feat(stock-trace): DLQ inspect/alert + manual replay endpoint with error-code whitelist"
```

---

## 阶段二（中优先级）

### Task 5: detect 并发/分批处理

**Files:**
- Modify: `aistock-app-api/src/modules/stock-trace/PriceTriggerDetector.ts:50-86`（detect 分批并发）
- Test: Create `aistock-app-api/src/modules/stock-trace/__tests__/price-detector.spec.ts`

**Interfaces:**
- Consumes: `StockTraceService.getFavoriteSecurities()`（已存在，`SELECT DISTINCT symbol`）、`TencentQuoteService.getBatchQuotes`（已存在）
- Produces: 导出纯函数 `splitIntoBatches<T>(items: T[], size: number): T[][]`（可单测）；`detect()` 按批次并发（上限 5）

- [ ] **Step 1: 写失败测试**

```typescript
// src/modules/stock-trace/__tests__/price-detector.spec.ts
// 运行：node --import tsx --test src/modules/stock-trace/__tests__/price-detector.spec.ts
import assert from 'node:assert/strict';
import { describe, it } from 'node:test';
import { splitIntoBatches } from '../PriceTriggerDetector';

describe('splitIntoBatches（detect 并发分批）', () => {
    it('按 size 分批且保留顺序', () => {
        assert.deepEqual(splitIntoBatches([1, 2, 3, 4, 5, 6, 7], 3), [[1, 2, 3], [4, 5, 6], [7]]);
    });
    it('空数组返回空', () => {
        assert.deepEqual(splitIntoBatches([], 5), []);
    });
    it('size 大于长度时单批', () => {
        assert.deepEqual(splitIntoBatches(['a', 'b'], 5), [['a', 'b']]);
    });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `node --import tsx --test src/modules/stock-trace/__tests__/price-detector.spec.ts`
Expected: FAIL（`splitIntoBatches` 未导出）。

- [ ] **Step 3: 实现**

`PriceTriggerDetector.ts` 顶部加常量，新增导出纯函数：

```typescript
const DETECT_CONCURRENCY = 5; // 并发分批上限：自选股互异，processPriceFact 内部有事务/UNIQUE 保护，可安全并发

export function splitIntoBatches<T>(items: T[], size: number): T[][] {
    const batches: T[][] = [];
    for (let index = 0; index < items.length; index += size) {
        batches.push(items.slice(index, index + size));
    }
    return batches;
}
```

`detect()` 的逐股循环改为分批并发（其余逻辑不变）：

```typescript
// 自选股已按 symbol DISTINCT，同股不会并发；不同 symbol 走各自事务与 UNIQUE 约束，安全。
for (const batch of splitIntoBatches(securities, DETECT_CONCURRENCY)) {
    await Promise.all(batch.map(async (security) => {
        const quote = quoteBySymbol.get(security.symbol);
        if (!quote) return;
        const latestPrice = numeric(quote[PRICE_FIELD]);
        const previousClose = numeric(quote[PREVIOUS_CLOSE_FIELD]);
        const changePct = numeric(quote[CHANGE_FIELD]);
        if (latestPrice === null || previousClose === null || previousClose <= 0 || changePct === null) return;
        const fact: PriceFact = {
            symbol: security.symbol,
            stockName: typeof quote[NAME_FIELD] === 'string' ? quote[NAME_FIELD] : security.stockName,
            latestPrice,
            previousClose,
            changePct,
            observedAt: now,
        };
        await StockTraceService.processPriceFact(security, fact);
    }));
}
```

> `this.running` 防重入不变：单次 `detect()` 内 symbols 互异，且不会有两个 `detect()` 同时运行，同股竞争风险不存在。

- [ ] **Step 4: 运行测试验证通过**

Run: `node --import tsx --test src/modules/stock-trace/__tests__/price-detector.spec.ts src/modules/stock-trace/__tests__/final-attribution.spec.ts`
Expected: PASS。

Run 类型检查：`npx tsc --noEmit` → 无错误。

- [ ] **Step 5: Commit**

```bash
git add src/modules/stock-trace/PriceTriggerDetector.ts src/modules/stock-trace/__tests__/price-detector.spec.ts
git commit -m "perf(stock-trace): batch-concurrent price detection (limit 5)"
```

---

### Task 6: 事件抓取 1-2 次指数退避重试

**Files:**
- Modify: `aistock-app-api/src/modules/stock-trace/StockTraceService.ts:74-101`（triggerEventScrape）
- Test: `aistock-app-api/src/modules/stock-trace/__tests__/eventScrapeTrigger.spec.ts`

**Interfaces:**
- Consumes: `process.env.AGENT_PY_URL`/`PYTHON_AGENT_URL`/`INTERNAL_API_TOKEN`（已存在）
- Produces: `triggerEventScrape` 最多 3 次尝试（首次 + 2 次退避 500ms/2s），仍失败仅日志不抛异常

- [ ] **Step 1: 写失败测试**

在 `eventScrapeTrigger.spec.ts` 末尾追加（沿用 mock fetch 惯例，用真实短延时验证重试次数；为不拖慢测试，退避间隔做成可注入参数）：

```typescript
it('fetch 失败重试 2 次后成功（共 3 次调用）', async () => {
    process.env.AGENT_PY_URL = 'http://agent:8080/';
    process.env.INTERNAL_API_TOKEN = 'tok';
    let call = 0;
    const calls: Array<{ url: string; init: RequestInit }> = [];
    mock.method(global, 'fetch', async (url: string | URL | Request, init?: RequestInit) => {
        call += 1;
        calls.push({ url: String(url), init: init ?? {} });
        if (call < 3) return { ok: false, status: 502 } as Response;
        return { ok: true } as Response;
    });

    await triggerEventScrape({
        event_id: 'e6', symbol: '600000', stock_name: '浦发银行',
        trading_date: '2026-08-12', direction: 'up' as never,
        first_triggered_at: new Date(), window_start_at: new Date(),
        window_end_at: new Date(), current_trigger_revision: 1,
        current_severity: 'high' as never, recovery_started_at: null,
    }, { retryDelaysMs: [0, 0] });

    assert.equal(calls.length, 3, '失败应重试 2 次');
});

it('fetch 全部失败仅记日志不抛异常', async () => {
    process.env.AGENT_PY_URL = 'http://agent:8080/';
    process.env.INTERNAL_API_TOKEN = 'tok';
    mock.method(global, 'fetch', async () => ({ ok: false, status: 500 }) as Response);

    await assert.doesNotReject(triggerEventScrape({
        event_id: 'e7', symbol: '000001', stock_name: '平安银行',
        trading_date: '2026-08-12', direction: 'up' as never,
        first_triggered_at: new Date(), window_start_at: new Date(),
        window_end_at: new Date(), current_trigger_revision: 1,
        current_severity: 'high' as never, recovery_started_at: null,
    }, { retryDelaysMs: [0, 0] }));
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `node --import tsx --test src/modules/stock-trace/__tests__/eventScrapeTrigger.spec.ts`
Expected: 新增 2 用例 FAIL（当前仅 1 次 fetch）。

- [ ] **Step 3: 实现**

`triggerEventScrape` 改为带重试；签名增加可选 `options` 供测试注入短延时（默认 500ms/2s）：

```typescript
const EVENT_SCRAPE_RETRY_DELAYS_MS = [500, 2000]; // 指数退避：500ms → 2s

export async function triggerEventScrape(
    event: EventRow,
    options: { retryDelaysMs?: number[] } = {},
): Promise<void> {
    const baseUrl = (process.env.AGENT_PY_URL || process.env.PYTHON_AGENT_URL || '').replace(/\/+$/, '');
    if (!baseUrl) return;
    const token = process.env.INTERNAL_API_TOKEN || '';
    if (!token || token === 'change-me-in-production') return;
    const delays = options.retryDelaysMs ?? EVENT_SCRAPE_RETRY_DELAYS_MS;
    let lastError: unknown = null;
    for (let attempt = 0; attempt <= delays.length; attempt += 1) {
        try {
            const response = await fetch(`${baseUrl}/api/agent/briefing/event-scrape/trigger`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'X-Internal-Token': token,
                },
                signal: AbortSignal.timeout(5000),
                body: JSON.stringify({
                    scrape_mode: 'event_triggered',
                    event: {
                        symbol: event.symbol,
                        score_date: event.trading_date,
                        windowStartAt: event.window_start_at,
                        windowEndAt: event.window_end_at,
                    },
                }),
            });
            if (response.ok) return;
            lastError = new Error(`event scrape HTTP ${response.status}`);
        } catch (error: unknown) {
            lastError = error;
        }
        if (attempt < delays.length) {
            await new Promise((resolve) => setTimeout(resolve, delays[attempt] as number));
        }
    }
    // 仍失败：告警日志，不阻断 stock_trace 主流程（快照采集有缺库降级路径兜底）
    console.error('[StockTrace] event scrape trigger failed after retries:', lastError instanceof Error ? lastError.message : String(lastError));
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `node --import tsx --test src/modules/stock-trace/__tests__/eventScrapeTrigger.spec.ts`
Expected: PASS（原有 5 用例 + 新增 2 用例）。

Run 类型检查：`npx tsc --noEmit` → 无错误。

- [ ] **Step 5: Commit**

```bash
git add src/modules/stock-trace/StockTraceService.ts src/modules/stock-trace/__tests__/eventScrapeTrigger.spec.ts
git commit -m "feat(stock-trace): retry event scrape with exponential backoff (500ms/2s)"
```

---

## 阶段三（按需 / 低优先级）

### Task 7: revision 增量差异采集

**Files:**
- Modify: `aistock-app-api/src/modules/stock-trace/StockTraceSnapshotService.ts`（`captureEnriched`/`captureCorrected` 增量 + `pickReusableSources` 导出 + `getLatestEnrichedForEvent`）
- Modify: `aistock-app-api/src/modules/stock-trace/StockTraceService.ts:381-388`（captureSnapshots 传增量开关）
- Test: Create `aistock-app-api/src/modules/stock-trace/__tests__/snapshot-incremental.spec.ts`

**Interfaces:**
- Consumes: `baseSources(event, now)`（已存在，产出当期 trigger_fact/quote_fact）、`collectCapitalSources`/`collectTechnicalSources`（已存在）、`getSnapshot(snapshotId)`（已存在）
- Produces: `pickReusableSources(records)` 纯函数（复用 company/sector/market，剔除 capital/technical/trigger/quote）；`captureEnriched(event, incremental=false)`、`captureCorrected(event, incremental=false)`、`scheduleEnriched(event, incremental=false)`

- [ ] **Step 1: 写失败测试**

```typescript
// src/modules/stock-trace/__tests__/snapshot-incremental.spec.ts
// 运行：node --import tsx --test src/modules/stock-trace/__tests__/snapshot-incremental.spec.ts
import assert from 'node:assert/strict';
import { describe, it } from 'node:test';
import { pickReusableSources } from '../StockTraceSnapshotService';
import type { StockSourceRecord } from '../types';

function rec(sourceId: string, kind: string): StockSourceRecord {
    return {
        sourceId, kind, provider: 't', sourceLevel: 'B', title: sourceId, contentExcerpt: '',
        symbol: '600000', capturedAt: new Date(), payload: {}, contentHash: sourceId,
    };
}

describe('pickReusableSources（增量采集复用域判定）', () => {
    it('复用 news/announcement/sector/market，剔除 capital/technical/trigger/quote', () => {
        const input = [
            rec('t1', 'trigger_fact'), rec('q1', 'quote_fact'),
            rec('n1', 'news'), rec('a1', 'announcement'),
            rec('s1', 'sector_fact'), rec('m1', 'market_fact'),
            rec('c1', 'capital_fact'), rec('k1', 'technical_fact'),
        ];
        const reused = pickReusableSources(input).map((r) => r.kind);
        assert.deepEqual(reused, ['news', 'announcement', 'sector_fact', 'market_fact']);
    });
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `node --import tsx --test src/modules/stock-trace/__tests__/snapshot-incremental.spec.ts`
Expected: FAIL（`pickReusableSources` 未导出）。

- [ ] **Step 3: 实现**

`StockTraceSnapshotService.ts` 顶部加常量与导出函数：

```typescript
// 增量采集复用域：盘中基本不变，修订时复用上一版本 enriched 快照；价格相关域重采
const INCREMENTAL_REUSE_KINDS = new Set(['news', 'announcement', 'sector_fact', 'market_fact']);

export function pickReusableSources(records: StockSourceRecord[]): StockSourceRecord[] {
    return records.filter((record) => INCREMENTAL_REUSE_KINDS.has(record.kind));
}
```

新增私有查询（取同 event 最近一次 enriched 快照，供修订复用；放 `getAnalysisContext` 附近）：

```typescript
private static async getLatestEnrichedForEvent(eventId: string): Promise<StockSourceRecord[] | null> {
    await this.ensureSchema();
    const result = await pool.query<{ snapshot_id: string }>(`
        SELECT snapshot_id FROM stock_trace_snapshots
        WHERE event_id = $1 AND snapshot_stage = 'enriched'
        ORDER BY captured_at DESC LIMIT 1
    `, [eventId]);
    if (!result.rows[0]) return null;
    const snapshot = await this.getSnapshot(result.rows[0].snapshot_id);
    if (!snapshot) return null;
    return snapshot.source_records as unknown as StockSourceRecord[];
}
```

`captureEnriched` 改造（保留原全量逻辑为 fallback）：

```typescript
static async captureEnriched(event: TriggerEvent, incremental = false): Promise<StockTraceSnapshot> {
    if (incremental) {
        const previousRecords = await this.getLatestEnrichedForEvent(event.eventId);
        if (previousRecords) {
            const capturedAt = new Date();
            const [capital, technical] = await Promise.allSettled([
                withinEnrichedBudget(this.collectCapitalSources(event, capturedAt)),
                withinEnrichedBudget(this.collectTechnicalSources(event, capturedAt)),
            ]);
            const sourceRecords = [
                ...this.baseSources(event, capturedAt),          // 当期 trigger/quote
                ...pickReusableSources(previousRecords),         // 复用 company/sector/market
                ...(capital.status === 'fulfilled' ? capital.value : []),
                ...(technical.status === 'fulfilled' ? technical.value : []),
            ];
            const readiness = buildDataReadiness([
                { layer: 'capital', count: capital.status === 'fulfilled' ? capital.value.length : 0 },
                { layer: 'technical', count: technical.status === 'fulfilled' ? technical.value.length : 0 },
            ]);
            const missingFields = Object.entries(readiness).filter(([, v]) => v !== 'complete').map(([k]) => `${k}_context`);
            return this.persist({ event, stage: 'enriched', capturedAt, sourceRecords, missingFields, dataReadiness: readiness });
        }
    }
    // 原全量五域采集逻辑保持不变
    const capturedAt = new Date();
    const [company, sector, market, capital, technical] = await Promise.allSettled([...]);
    // ...（现有代码原样保留）
}
```

`captureCorrected` 同步加增量开关：

```typescript
static async captureCorrected(event: TriggerEvent, incremental = false): Promise<StockTraceSnapshot> {
    if (incremental) {
        const previousRecords = await this.getLatestEnrichedForEvent(event.eventId);
        if (previousRecords) {
            const capturedAt = new Date();
            const [capital, technical] = await Promise.allSettled([
                withinEnrichedBudget(this.collectCapitalSources(event, capturedAt)),
                withinEnrichedBudget(this.collectTechnicalSources(event, capturedAt)),
            ]);
            const sourceRecords = [
                ...this.baseSources(event, capturedAt),
                ...pickReusableSources(previousRecords),
                ...(capital.status === 'fulfilled' ? capital.value : []),
                ...(technical.status === 'fulfilled' ? technical.value : []),
            ];
            const readiness = buildDataReadiness([
                { layer: 'capital', count: capital.status === 'fulfilled' ? capital.value.length : 0 },
                { layer: 'technical', count: technical.status === 'fulfilled' ? technical.value.length : 0 },
            ]);
            const missingFields = Object.entries(readiness).filter(([, v]) => v !== 'complete').map(([k]) => `${k}_context`);
            return this.persist({ event, stage: 'corrected', capturedAt, sourceRecords, missingFields, dataReadiness: readiness });
        }
    }
    const initial = await this.captureInitialForStage(event, 'corrected');
    return initial;
}
```

> 复用域缺失（如第一次修订时还没有 enriched）时走全量 fallback；`persist` 的 UNIQUE hash 幂等不变。

`StockTraceService.captureSnapshots` 传增量开关：

```typescript
private static captureSnapshots(event: TriggerEvent, mutation: 'created' | 'revised'): void {
    const incremental = mutation === 'revised';
    const capture = mutation === 'created'
        ? StockTraceSnapshotService.captureInitial(event)
        : StockTraceSnapshotService.captureCorrected(event, incremental);
    void capture.then(() => StockTraceSnapshotService.scheduleEnriched(event, incremental)).catch((error: unknown) => {
        console.error('[StockTrace] snapshot capture failed:', error instanceof Error ? error.message : error);
    });
}
```

> `scheduleEnriched(event, incremental)` 已在 Task 1 改造过签名（`incremental = false`），此处直接透传。

- [ ] **Step 4: 运行测试验证通过**

Run: `node --import tsx --test src/modules/stock-trace/__tests__/snapshot-incremental.spec.ts src/modules/stock-trace/__tests__/snapshotDomain.spec.ts`
Expected: PASS。

Run 全量回归 + 类型检查：

```bash
node --import tsx --test src/modules/stock-trace/__tests__/*.spec.ts
npx tsc --noEmit
```

Expected: 全部 PASS；tsc 无错误。

- [ ] **Step 5: Commit**

```bash
git add src/modules/stock-trace/StockTraceSnapshotService.ts src/modules/stock-trace/StockTraceService.ts src/modules/stock-trace/__tests__/snapshot-incremental.spec.ts
git commit -m "perf(stock-trace): incremental revision snapshot reuse company/sector/market"
```

---

### Task 8: `/metrics` 指标端点 + Stream/DLQ 观测

**Files:**
- Modify: `aistock-agent-py/src/aistock_agent/api/routes.py`（`GET /metrics`）
- Test: `aistock-agent-py/tests/unit/test_stock_trace.py`（对 `_metrics` 计数与端点的轻量断言，可选）

**Interfaces:**
- Consumes: `get_metrics()`（observability/metrics.py，Task 2 已扩展 stock_trace 计数）、`settings.stock_trace_redis_url`
- Produces: `GET /metrics`（JSON，含既有指标 + stock_trace 计数 + 实时 Stream lag / DLQ 长度 / pending 数）

- [ ] **Step 1: 写失败测试**

```python
@pytest.mark.asyncio
async def test_metrics_endpoint_returns_stock_trace_gauges() -> None:
    from aistock_agent.observability.metrics import get_metrics_collector
    from aistock_agent.api.routes import get_stock_trace_observability

    collector = get_metrics_collector()
    collector.reset()
    collector.record_stock_trace_snapshot_not_ready()
    collector.record_stock_trace_validation_failed("StockTraceValidationError")
    collector.record_stock_trace_dlq_total("SNAPSHOT_TIMEOUT")

    snap = get_stock_trace_observability()
    assert snap["stock_trace"]["snapshot_not_ready_total"] == 1
    assert snap["stock_trace"]["validation_failed_total"]["StockTraceValidationError"] == 1
    assert snap["stock_trace"]["dlq_total"]["SNAPSHOT_TIMEOUT"] == 1
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/unit/test_stock_trace.py -v`
Expected: FAIL（`get_stock_trace_observability` 未定义）。

- [ ] **Step 3: 实现**

`routes.py` 新增可注入的观测函数（放在健康检查段之前）：

```python
async def get_stock_trace_observability() -> dict[str, object]:
    """组装 stock_trace 观测快照：进程内计数器 + Redis 实时 gauge。

    计数器来自 MetricsCollector（Task 2 扩展）；gauge 实时读 Redis（stream lag /
    DLQ 长度 / 未确认 pending），读失败以降级值返回，不阻塞 /metrics。
    """
    from aistock_agent.observability.metrics import get_metrics
    from aistock_agent.workers.stock_trace_consumer import DLQ_STREAM, STREAM

    snapshot = get_metrics()
    stock_trace = dict(snapshot.get("stock_trace", {}))
    gauges: dict[str, object] = {"stream_lag": 0, "dlq_length": 0, "pending_unacked": 0}
    try:
        redis_client = aioredis.from_url(settings.stock_trace_redis_url)
        try:
            gauges["dlq_length"] = await redis_client.xlen(DLQ_STREAM)
            groups = await redis_client.xinfo_groups(STREAM)
            if groups:
                gauges["stream_lag"] = groups[0].get("lag", 0)
            pending = await redis_client.xpending(STREAM, settings.stock_trace_consumer_group)
            if pending:
                gauges["pending_unacked"] = pending[0]
        finally:
            await redis_client.aclose()
    except Exception:
        logger = structlog.get_logger()
        logger.warning("metrics_redis_read_failed")
    return {"stock_trace": {**stock_trace, **gauges}}


@health_router.get("/metrics")
async def metrics() -> dict[str, object]:
    """观测指标（Prometheus 语义的 JSON 快照）。含 stock_trace 计数与实时 gauge。"""
    return await get_stock_trace_observability()
```

> 若 `aioredis` 在 routes.py 未导入，函数内 `import redis.asyncio as aioredis`。

- [ ] **Step 4: 运行测试验证通过**

Run: `python -m pytest tests/unit/test_stock_trace.py -v`
Expected: PASS（新增用例 + 既有用例）。

- [ ] **Step 5: Commit**

```bash
git add src/aistock_agent/api/routes.py tests/unit/test_stock_trace.py
git commit -m "feat(stock-trace): /metrics endpoint with stream lag / DLQ / validation gauges"
```

---

## 收尾

### Task 9: 文档与修改记录

**Files:**
- Modify: `aistock-app-api/src/modules/stock-trace/AGENTS.md`（追加优化决策）
- Modify: `aistock-agent-py/changelog-pending.md`、`aistock-app-api/changelog-pending.md`（各仓库一次性追加）
- Modify: `aistock-agent-py/docs/异动捕手-优化点评估与设计方案.md`（在 §6 落地顺序后标注已完成项，可选）

- [ ] **Step 1: 更新模块 AGENTS.md**

在 `aistock-app-api/src/modules/stock-trace/AGENTS.md` 末尾追加：

```markdown
### 2026-08-21 更新：优化实施（发布 gate / 并发检测 / 抓取重试 / 增量采集）

- **发布 gate**：`StockTraceJobService.publishPending` 发布前检查 `(event_id, trigger_revision)` 的 enriched 快照是否就绪；未就绪置 `held_until`（5s 重查、60s 硬超时后强制发布交由 consumer 兜底）。outbox 新增 `held_until` 列。`scheduleEnriched` 完成后自动冲刷 outbox。
- **落定路径就绪兜底**：`enqueueFinalAnalysis`（无 client 路径）入队前 `ensureEnrichedReady`——enriched 缺失时同步采集一次；反向落定（事务内）不阻塞等待，靠 consumer 超时兜底。
- **detect 并发**：`PriceTriggerDetector.detect` 按批次并发（上限 5），导出 `splitIntoBatches`；自选股 DISTINCT 保证同股不并发，事务 + UNIQUE 约束保写入安全。
- **抓取重试**：`triggerEventScrape` 失败指数退避重试（500ms/2s，共 3 次），仍失败仅告警不阻断。
- **增量采集**：revision 修订时 `captureEnriched`/`captureCorrected` 增量——复用上一版本 enriched 的 company/sector/market 域，仅重采 capital/technical（`pickReusableSources`），减少 Tushare/东财请求量。
```

- [ ] **Step 2: 追加 changelog**

`aistock-agent-py/changelog-pending.md` 与 `aistock-app-api/changelog-pending.md` 各追加一段（按各仓库既有格式），内容覆盖本次 8 个任务的实现清单与文件路径，并注明「分三阶段实施，见 `docs/异动捕手-优化实施计划.md`」。

- [ ] **Step 3: 更新设计方案（可选）**

在 `异动捕手-优化点评估与设计方案.md` 的 §6 落地顺序表后加一行状态注记：优化 1/2/3/4/6/8 已按计划实施（Task 1-8），优化 7 无需改动。

- [ ] **Step 4: 全量回归**

```bash
# aistock-app-api
node --import tsx --test src/modules/stock-trace/__tests__/*.spec.ts
npx tsc --noEmit
# aistock-agent-py
python -m pytest tests/unit/test_stock_trace.py tests/test_stock_trace_validator.py -v
```

Expected: 全部 PASS、tsc 无错误。

- [ ] **Step 5: Commit**

```bash
# 两个仓库各自提交文档改动
git add src/modules/stock-trace/AGENTS.md
git commit -m "docs(stock-trace): record optimization implementation decisions"
git add changelog-pending.md
git commit -m "docs: append stock-trace optimization changelog"
```

---

## 验证矩阵（阶段验收）

| 任务 | 命令 | 期望 |
|------|------|------|
| T1 | `node --import tsx --test src/modules/stock-trace/__tests__/publish-gate.spec.ts` | 2 用例 PASS |
| T2/T3/T4 | `python -m pytest tests/unit/test_stock_trace.py -v` | 全部 PASS |
| T5 | `node --import tsx --test src/modules/stock-trace/__tests__/price-detector.spec.ts` | 3 用例 PASS |
| T6 | `node --import tsx --test src/modules/stock-trace/__tests__/eventScrapeTrigger.spec.ts` | 7 用例 PASS |
| T7 | `node --import tsx --test src/modules/stock-trace/__tests__/snapshot-incremental.spec.ts` | 1 用例 PASS |
| T8 | `python -m pytest tests/unit/test_stock_trace.py -v` | 新增用例 PASS |
| 全量 | `node --import tsx --test src/modules/stock-trace/__tests__/*.spec.ts` + `npx tsc --noEmit` + `python -m pytest tests/unit/test_stock_trace.py tests/test_stock_trace_validator.py -v` | 全部 PASS |

## 风险与回滚

- **Task 1 gate 误判**：若 `checkEnrichedSnapshotReady` 查询本身报错，会进 `publishPending` 的 catch → 记 failed 不发布（与现状一致）；60s 硬超时强制发布保证不永久卡死。回滚：删 `held_until` 过滤条件即可。
- **Task 3 重试 token**：每次校验失败 +1 次 LLM 调用；用 `stock_trace_validation_failed_total` 指标监控，持续偏高优先修 prompt/schema 而非加重试。
- **Task 4 重投风暴**：白名单 + limit 50 + 路由 400 拦截；结构性错误码永不重投。
- **Task 5 并发**：批次内 symbols 互异（DISTINCT），`this.running` 防重入；异常由 `processPriceFact` 各自事务回滚，单股失败不影响他股。
- **Task 7 复用域**：company/sector/market 取自同 event 最近 enriched，若盘中重大新闻在两次修订间出现可能漏采——仅影响增量场景，最终 enriched 仍五域齐全（baseSources 当期 + 复用 + 重采）。
